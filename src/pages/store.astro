---
import Layout from '../layouts/Layout.astro';

const API_BASE_URL = 'https://qus-bot-2-production.up.railway.app';

let products = [];

try {
  const response = await fetch(`${API_BASE_URL}/api/products`);
  const data = await response.json();
  
  if (data.success && data.products) {
    products = data.products.map(product => ({
      id: product.id,
      name: product.name,
      description: product.description || `Professional development service - ${product.name}`,
      price: product.price || 150.00,
      originalPrice: product.originalPrice || product.price * 1.67,
      features: product.features || [
        "Professional quality",
        "Full documentation", 
        "Performance optimized",
        "Source code included",
        "Support included",
        "Custom implementation"
      ],
      image: product.image || "https://res.cloudinary.com/dm0o7bd4t/image/upload/v1752895789/Logo_zpxvlb.jpg",
      category: product.category || "script",
      popular: product.popular || false,
      link: product.link,
      video_data: product.video_data
    }));
  }
} catch (error) {
  console.error('Failed to fetch products data:', error);
  products = [
    {
      id: 1,
      name: "Custom Development Service",
      description: "Professional Roblox development services available.",
      price: 150.00,
      originalPrice: 250.00,
      features: [
        "Professional quality",
        "Full documentation",
        "Performance optimized",
        "Source code included",
        "Support included",
        "Custom implementation"
      ],
      image: "https://res.cloudinary.com/dm0o7bd4t/image/upload/v1752895789/Logo_zpxvlb.jpg",
      category: "service",
      popular: true
    }
  ];
}
---

<Layout title="Store - Qustreso Studios" description="Professional Roblox development products and services. Get custom scripts, frameworks, and development services." currentPage="store">
  
  <section class="pt-24 pb-16">
    <div class="max-w-4xl mx-auto text-center px-4 sm:px-6 lg:px-8">
      <h1 class="text-4xl sm:text-5xl font-bold text-white mb-6">
        Professional <span class="bg-gradient-to-r from-primary-400 to-secondary-400 bg-clip-text text-transparent">Shop</span>
      </h1>
      <p class="text-xl text-gray-300 mb-8">
        Get professional-grade Roblox development products and services from a 16-year-old prodigy. 
        Quality scripts, frameworks, and custom development services that have earned $10,000+ in commissions.
      </p>
      <div class="flex items-center justify-center gap-6 text-sm text-gray-400">
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-primary-400" fill="currentColor" viewBox="0 0 24 24">
            <path d="M16 4c0-1.11.89-2 2-2s2 .89 2 2-.89 2-2 2-2-.89-2-2zm4 18v-6h2.5l-2.54-7.63A1.5 1.5 0 0 0 18.54 8H17c-.8 0-1.54.37-2.01 1l-1.7 2.26V15h-1.5v-3H9v3H7.5V9.5l-1.96-2.61C5.11 6.37 4.37 6 3.57 6H2.46c-.8 0-1.54.37-2.01 1L0 12.5V22h16z"/>
          </svg>
          <span>50+ Happy Clients</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
          </svg>
          <span>4.9/5 Rating</span>
        </div>
        <div class="flex items-center gap-2">
          <svg class="w-5 h-5 text-green-400" fill="currentColor" viewBox="0 0 24 24">
            <path d="M11.99 2C6.47 2 2 6.48 2 12s4.47 10 9.99 10C17.52 22 22 17.52 22 12S17.52 2 11.99 2zM12 20c-4.42 0-8-3.58-8-8s3.58-8 8-8 8 3.58 8 8-3.58 8-8 8zm.5-13H11v6l5.25 3.15.75-1.23-4.5-2.67z"/>
          </svg>
          <span>Instant Delivery</span>
        </div>
      </div>
    </div>
  </section>

  <section class="py-16">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="glass-primary rounded-2xl p-8 md:p-12 border border-primary-400/30">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
          <div>
            <h2 class="text-3xl font-bold text-white mb-6">
              From 16-Year-Old Developer to Industry Leader
            </h2>
            <p class="text-lg text-gray-300 mb-6">
              At just 16 years old, I've already achieved what many developers spend years trying to accomplish. 
              I've closed over $10,000 in commissions, contributed to games with 3.1M+ visits, and built a reputation 
              for delivering professional-quality Roblox development services.
            </p>
            <p class="text-lg text-gray-300 mb-6">
              Now, I'm offering professional-grade development products and services to help you build amazing games. 
              Get the same quality and expertise that has made me successful in the competitive Roblox development market.
            </p>
            <div class="flex flex-wrap gap-4">
              <div class="flex items-center gap-2 text-sm text-gray-300">
                <svg class="w-4 h-4 text-green-400" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
                </svg>
                <span>2+ Years Experience</span>
              </div>
              <div class="flex items-center gap-2 text-sm text-gray-300">
                <svg class="w-4 h-4 text-green-400" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
                </svg>
                <span>50+ Happy Clients</span>
              </div>
              <div class="flex items-center gap-2 text-sm text-gray-300">
                <svg class="w-4 h-4 text-green-400" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
                </svg>
                <span>3.1M+ Game Visits</span>
              </div>
            </div>
          </div>
          <div class="text-center">
            <img src="https://res.cloudinary.com/dm0o7bd4t/image/upload/v1752895789/Logo_zpxvlb.jpg" alt="Qustreso Studios" class="w-64 h-64 mx-auto rounded-2xl object-cover border border-primary-400/30">
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex flex-wrap justify-center gap-4 mb-12">
        <button class="filter-btn active" data-filter="all">
          All Products
        </button>
        <button class="filter-btn" data-filter="script">
          Scripts
        </button>
        <button class="filter-btn" data-filter="framework">
          Frameworks
        </button>
        <button class="filter-btn" data-filter="service">
          Services
        </button>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6" id="products-grid">
        {products.map((product) => (
          <div class="product-card group" data-category={product.category}>
            {product.popular && (
              <div class="popular-badge">
                <span>Most Popular</span>
              </div>
            )}
            
            <div class="product-media">
              {product.video_data?.type === 'youtube' && product.video_data?.youtube_id ? (
                <iframe 
                  src={`https://www.youtube.com/embed/${product.video_data.youtube_id}`}
                  title={product.name}
                  class="w-full h-48 rounded-t-2xl"
                  frameborder="0"
                  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                  allowfullscreen>
                </iframe>
              ) : product.video_data?.type === 'direct' && product.video_data?.url ? (
                <video 
                  class="w-full h-48 rounded-t-2xl object-cover"
                  controls
                  preload="metadata"
                >
                  <source src={product.video_data.url} type="video/mp4">
                  Your browser does not support the video tag.
                </video>
              ) : (
                <img src={product.image} alt={product.name} class="w-full h-48 object-cover rounded-t-2xl">
              )}
            </div>
            
            <div class="product-content">
              <h3 class="product-title">{product.name}</h3>
              <p class="product-description">{product.description}</p>
              
              <div class="product-features">
                <ul class="space-y-2">
                  {product.features.slice(0, 3).map((feature) => (
                    <li class="flex items-center gap-2 text-sm text-gray-300">
                      <svg class="w-4 h-4 text-green-400" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
                      </svg>
                      {feature}
                    </li>
                  ))}
                  {product.features.length > 3 && (
                    <li class="text-sm text-primary-400 font-medium">
                      +{product.features.length - 3} more features
                    </li>
                  )}
                </ul>
              </div>
              
              <div class="product-pricing">
                <div class="price-container">
                  <span class="current-price">${product.price}</span>
                  {product.originalPrice > product.price && (
                    <span class="original-price">${product.originalPrice}</span>
                  )}
                </div>
                <div class="discount-badge">
                  {product.originalPrice > product.price && (
                    <span>{Math.round(((product.originalPrice - product.price) / product.originalPrice) * 100)}% OFF</span>
                  )}
                </div>
              </div>
              
              <button class="buy-btn" onclick={product.link ? `window.open('${product.link}', '_blank')` : `window.open('https://discord.gg/qustreso', '_blank')`}>
                <span>Get Started</span>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
                </svg>
              </button>
            </div>
          </div>
        ))}
      </div>
      
      {products.length === 0 && (
        <div class="text-center py-16" id="empty-state">
          <p class="text-xl text-gray-400">No products available at the moment.</p>
          <p class="text-gray-500 mt-2">Check back soon for new development services!</p>
        </div>
      )}
    </div>
  </section>

  <section class="py-16">
    <div class="max-w-4xl mx-auto text-center px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold text-white mb-6">Need Something Custom?</h2>
      <p class="text-xl text-gray-300 mb-8">
        Don't see exactly what you need? I offer custom development services tailored to your specific requirements.
      </p>
      <a href="/qus/hire" class="btn-primary">
        <span>Request Custom Quote</span>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
        </svg>
      </a>
    </div>
  </section>
</Layout>

<style>
  .btn-primary {
    @apply inline-flex items-center gap-2 px-8 py-3 bg-gradient-to-r from-primary-500 to-secondary-500 text-white font-semibold rounded-lg hover:from-primary-600 hover:to-secondary-600 transform hover:scale-105 transition-all duration-300 shadow-lg hover:shadow-xl;
  }
  
  .filter-btn {
    @apply px-6 py-3 rounded-xl text-gray-300 hover:text-white transition-all duration-300 font-medium border border-gray-600/30 hover:border-primary-400/50 hover:bg-gray-800/50 backdrop-blur-sm;
  }
  
  .filter-btn.active {
    @apply bg-gradient-to-r from-primary-500 to-secondary-500 text-white border-primary-400/50 shadow-lg shadow-primary-500/25;
  }
  
  .product-card {
    @apply relative rounded-2xl overflow-hidden transition-all duration-500;
    background: linear-gradient(145deg, rgba(30, 41, 59, 0.8), rgba(15, 23, 42, 0.9));
    border: 1px solid rgba(59, 130, 246, 0.2);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3), 0 1px 8px rgba(59, 130, 246, 0.1);
    backdrop-filter: blur(10px);
    transform: scale(1) translateY(0);
  }
  
  .product-card:hover {
    transform: scale(1.02) translateY(-8px);
  }
  
  .product-card:hover {
    border-color: rgba(59, 130, 246, 0.4);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4), 0 8px 32px rgba(59, 130, 246, 0.2);
  }
  
  .popular-badge {
    @apply absolute top-4 right-4 z-20;
    background: linear-gradient(135deg, #f59e0b, #d97706);
    color: white;
    font-size: 11px;
    font-weight: 700;
    padding: 6px 12px;
    border-radius: 20px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    box-shadow: 0 4px 12px rgba(245, 158, 11, 0.4);
    animation: pulse 2s infinite;
  }
  
  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }
  
  .product-media {
    @apply relative overflow-hidden;
    height: 200px;
  }
  
  .product-media img,
  .product-media iframe,
  .product-media video {
    @apply w-full h-full object-cover transition-transform duration-500;
  }
  
  .product-card:hover .product-media img,
  .product-card:hover .product-media iframe,
  .product-card:hover .product-media video {
    transform: scale(1.1);
  }
  
  .product-media::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 40px;
    background: linear-gradient(transparent, rgba(15, 23, 42, 0.8));
    pointer-events: none;
  }
  
  .product-content {
    @apply p-6 space-y-4;
  }
  
  .product-title {
    @apply text-xl font-bold text-white mb-2 leading-tight;
    background: linear-gradient(135deg, #ffffff, #e2e8f0);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  
  .product-description {
    @apply text-gray-300 text-sm leading-relaxed mb-4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  .product-features ul {
    @apply space-y-3;
  }
  
  .product-features li {
    @apply flex items-center gap-3 text-sm text-gray-200 transition-colors duration-200 hover:text-white;
  }
  
  .product-features svg {
    @apply w-4 h-4 text-emerald-400 flex-shrink-0;
    filter: drop-shadow(0 2px 4px rgba(16, 185, 129, 0.3));
  }
  
  .product-features .text-primary-400 {
    @apply text-cyan-400 font-semibold hover:text-cyan-300 transition-colors duration-200;
  }
  
  .product-pricing {
    @apply flex items-center justify-between py-4 px-2 rounded-xl;
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(16, 185, 129, 0.1));
    border: 1px solid rgba(59, 130, 246, 0.2);
  }
  
  .price-container {
    @apply flex items-baseline gap-3;
  }
  
  .current-price {
    @apply text-3xl font-black text-white;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
  }
  
  .original-price {
    @apply text-lg text-gray-400 line-through font-medium;
  }
  
  .discount-badge {
    @apply bg-gradient-to-r from-emerald-500 to-teal-500 text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg;
    box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
  }
  
  .buy-btn {
    @apply w-full font-bold py-4 px-6 rounded-xl transition-all duration-300 flex items-center justify-center gap-3 text-white relative overflow-hidden;
    background: linear-gradient(135deg, #3b82f6, #1e40af, #1d4ed8);
    box-shadow: 0 8px 25px rgba(59, 130, 246, 0.4);
    border: 1px solid rgba(59, 130, 246, 0.3);
  }
  
  .buy-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s;
  }
  
  .buy-btn:hover::before {
    left: 100%;
  }
  
  .buy-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 35px rgba(59, 130, 246, 0.6);
    background: linear-gradient(135deg, #2563eb, #1d4ed8, #1e3a8a);
  }
  
  .buy-btn:hover svg {
    transform: translateX(4px);
  }
  
  .buy-btn svg {
    @apply transition-transform duration-300;
  }
  
  .buy-btn span {
    @apply relative z-10;
  }
</style>

<script>
  const API_BASE = 'https://qus-bot-2-production.up.railway.app';
  
  function createProductCard(product) {
    const discountPercent = product.originalPrice > product.price ? 
      Math.round(((product.originalPrice - product.price) / product.originalPrice) * 100) : 0;
    
    const featuresArray = product.features || ['Professional quality', 'Full documentation', 'Performance optimized'];
    const videoData = product.video_data || {};
    
    let mediaHTML = '';
    if (videoData.type === 'youtube' && videoData.youtube_id) {
      mediaHTML = `
        <iframe 
          src="https://www.youtube.com/embed/${videoData.youtube_id}"
          title="${product.name}"
          class="w-full h-48 rounded-t-2xl"
          frameborder="0"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen>
        </iframe>
      `;
    } else if (videoData.type === 'direct' && videoData.url) {
      mediaHTML = `
        <video class="w-full h-48 rounded-t-2xl object-cover" controls preload="metadata">
          <source src="${videoData.url}" type="video/mp4">
          Your browser does not support the video tag.
        </video>
      `;
    } else {
      mediaHTML = `<img src="${product.image || 'https://res.cloudinary.com/dm0o7bd4t/image/upload/v1752895789/Logo_zpxvlb.jpg'}" alt="${product.name}" class="w-full h-48 object-cover rounded-t-2xl">`;
    }
    
    return `
      <div class="product-card group" data-category="${product.category || 'script'}">
        ${product.popular ? '<div class="popular-badge"><span>Most Popular</span></div>' : ''}
        
        <div class="product-media">${mediaHTML}</div>
        
        <div class="product-content">
          <h3 class="product-title">${product.name}</h3>
          <p class="product-description">${product.description || `Professional development service - ${product.name}`}</p>
          
          <div class="product-features">
            <ul class="space-y-2">
              ${featuresArray.slice(0, 3).map(feature => `
                <li class="flex items-center gap-2 text-sm text-gray-300">
                  <svg class="w-4 h-4 text-green-400" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
                  </svg>
                  ${feature}
                </li>
              `).join('')}
              ${featuresArray.length > 3 ? `
                <li class="text-sm text-primary-400 font-medium">
                  +${featuresArray.length - 3} more features
                </li>
              ` : ''}
            </ul>
          </div>
          
          <div class="product-pricing">
            <div class="price-container">
              <span class="current-price">${product.price || 150}</span>
              ${product.originalPrice > product.price ? `<span class="original-price">${product.originalPrice}</span>` : ''}
            </div>
            ${discountPercent > 0 ? `<div class="discount-badge"><span>${discountPercent}% OFF</span></div>` : ''}
          </div>
          
          <button class="buy-btn" onclick="window.open('${product.link || 'https://discord.gg/qustreso'}', '_blank')">
            <span>Get Started</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </button>
        </div>
      </div>
    `;
  }
  
  function updateProductsGrid(products) {
    const grid = document.getElementById('products-grid');
    const emptyState = document.getElementById('empty-state');
    
    if (!grid || !products) return;
    
    if (products.length === 0) {
      grid.innerHTML = '';
      if (emptyState) emptyState.style.display = 'block';
      return;
    }
    
    if (emptyState) emptyState.style.display = 'none';
    grid.innerHTML = products.map(createProductCard).join('');
    
    attachFilterListeners();
  }
  
  function attachFilterListeners() {
    const filterBtns = document.querySelectorAll('.filter-btn');
    const productCards = document.querySelectorAll('.product-card');
    
    filterBtns.forEach(btn => {
      btn.removeEventListener('click', handleFilterClick);
      btn.addEventListener('click', handleFilterClick);
    });
    
    function handleFilterClick(e) {
      const filter = e.target.getAttribute('data-filter');
      
      filterBtns.forEach(b => b.classList.remove('active'));
      e.target.classList.add('active');
      
      productCards.forEach(card => {
        const category = card.getAttribute('data-category');
        if (filter === 'all' || category === filter) {
          card.style.display = 'block';
        } else {
          card.style.display = 'none';
        }
      });
    }
  }
  
  async function updateContent() {
    try {
      const productsResponse = await fetch(`${API_BASE}/api/products`);
      const productsData = await productsResponse.json();
      
      if (productsData.success && productsData.products && productsData.products.length > 0) {
        updateProductsGrid(productsData.products);
      }
      
    } catch (error) {
      return;
    }
  }
  
  document.addEventListener('DOMContentLoaded', function() {
    updateContent();
    setInterval(updateContent, 30000);
    
    document.addEventListener('visibilitychange', function() {
      if (!document.hidden) {
        updateContent();
      }
    });
    
    attachFilterListeners();
  });
</script>
